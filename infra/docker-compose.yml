services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: agent_eval
      POSTGRES_PASSWORD: agent_eval
      POSTGRES_DB: agent_eval_dev
    ports:
      - "5432:5432"
    volumes:
      - agent_eval_pgdata:/var/lib/postgresql/data

volumes:
  agent_eval_pgdata:
